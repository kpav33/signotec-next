interface STPadServerLibType {
  STPadServerLibDefault: any; // Replace 'any' with the actual type if known
  STPadServerLibCommons: any; // Replace 'any' with the actual type if known
  STPadServerLibApi: any; // Replace 'any' with the actual type if known
}

interface Window {
  STPadServerLib: STPadServerLibType;
}
